function getdate(type) {
    let today=new Date()
    let tomonth=today.getMonth()+1
    let todate=today.today.getDate()
    if(type === 0) document.write(tomonth);
    if(type === 1) document.write(todate);
}